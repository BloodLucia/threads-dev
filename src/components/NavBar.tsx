interface Props {
  leftIcon?: React.ReactNode
  rightIcon?: React.ReactNode
}
export const NavBar: React.FC<Props> = ({  }) => {
  return (
    <div className="w-full h-[60px] py-[12px] flex"></div>
  )
}